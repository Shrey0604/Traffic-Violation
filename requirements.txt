# Core vision & numerics
opencv-python>=4.8.0
numpy>=1.24.0

# YOLO inference (ultralytics is the recommended backend)
ultralytics>=8.0.0

# OCR (easyocr preferred; pytesseract as fallback)
easyocr>=1.7.0
# pytesseract>=0.3.10   # uncomment if easyocr is unavailable
